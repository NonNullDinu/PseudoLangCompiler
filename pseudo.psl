declare int, a, b, c, i, j, z, k=0
begin
	read a, b, c
	for i <- a, b do
	begin
		for j <- i, c - i do
		begin
			z <- c - i - j
			if(j <= z && z <= b) then
			begin
				k <- k + 1
				write "{", i, ",", j, ",", z, "}", "\n"
			end
		end
	end
	if(k == 0)then write "doesn't exist", "\n"
end
exit