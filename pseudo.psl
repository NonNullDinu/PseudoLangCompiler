begin
    int a
    file_stream fl
    int i
    open file fl, "pseudo.in", read only, 744
    i <- 1
    while (i <= 9)
    do begin
        read file fl, a
        write a
        write "\n"
        i <- i + 2
    end
    close file fl
end
exit